{
  "distance_influence": 1,
  "priority": [
    {
      "if": "road_access == PRIVATE",
      "multiply_by": "0"
    },
    {
      "if": "hgv == NO || max_width < 3 || max_height < 4 || max_weight < 20",
      "multiply_by": "0"
    }
  ],
  "speed": [
    {
      "if": "true",
      "limit_to": "car_average_speed * 0.8"
    },
    {
      "if": "true",
      "limit_to": "80"
    }
  ]
}
